interface;
use draft;

define behavior for Z_I_TRAVEL_FJCM alias Customer
{
  use create;
  use update;
  use delete;

  use action acceptTravel;
  use action rejectTravel;
  use action deductDiscount;
  use action validateCustomerID;
  use action validateAgencyID;
  use action validateDates;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}