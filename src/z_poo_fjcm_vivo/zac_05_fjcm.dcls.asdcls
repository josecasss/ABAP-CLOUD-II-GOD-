@EndUserText.label: 'Full Access'
@MappingRole: true
define role ZAC_05_FJCM {
    grant
        select
            on
                Z363_18_FJCM; 
                
}

// Made with template RoleWithUnstrictedAccess

// This cds had a AccessControl with Authorization object, but this Full Access have more PRIORITY since CDS allways give more priority to AccessControl with more data to see